# External libraries to build binary models

To build and install them, you can use `make` command.

```
make
```

By default, install directory is `deploy/install`. If you'd like to specify the install directory, you can use parameters like this:

```
make INSTALL_PREFIX=/opt/pfn/pfp/deploy
```
